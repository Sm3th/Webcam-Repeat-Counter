# docs/

Placeholders for marketing assets referenced by the root `README.md`:

- `demo.gif` — short screen recording of the rep counter in action.
- `screenshot.png` — a still of the main UI.

Record these against a deployed build and drop them here.
